# Plant: 1C RAC UserParameters — Linux example
# Adjust paths, cluster address, and the OS user allowed to run rac.
# Recommended: run the script as a dedicated low-privilege user with read-only access.

UserParameter=plant.1c.rac.available,/opt/zabbix/scripts/1c_rac_available.sh
UserParameter=plant.1c.sessions.count,/opt/zabbix/scripts/1c_sessions_count.sh
